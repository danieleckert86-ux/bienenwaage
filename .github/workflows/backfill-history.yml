name: Manual archive maintenance
on:
  workflow_dispatch:
jobs:
  instructions:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Use npm run backfill with DATABASE_URL and explicit dates. See README. No automatic historical rewrite.'
